on:
  push:
    branches:
      - master
  pull_request: {}
  workflow_call:
    inputs:
      use-haskell-debugger-view-from-hackage:
        required: true
        type: boolean
      is-release:
        required: true
        type: boolean

env:
  haskellDebuggerViewVersion: 0.2.1.0
  haskellDebuggerVersion: 0.13.1.0
  USE_HASKELL_DEBUGGER_VIEW_FROM_HACKAGE: ${{
      github.event_name == 'workflow_call'
        && inputs.use-haskell-debugger-view-from-hackage
        || false
    }}

name: Debugger CI
jobs:
  build-vscode-extension:
    name: Build VSCode Extension
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - name: Build extension
        run: |
          cd vscode-extension/
          nix-build

      - name: Upload extension artifact
        uses: actions/upload-artifact@v4
        with:
          name: Haskell Debugger Extension
          path: vscode-extension/result/haskell-debugger-extension*.vsix

  sdist-haskell-debugger:
    name: Sdist
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: haskell-actions/setup@v2

      - name: Package hdb
        run: cabal sdist

      - name: Package haskell-debugger-view
        run: cabal sdist haskell-debugger-view

      - name: Upload haskell-debugger source distribution
        uses: actions/upload-artifact@v4
        with:
          name: Haskell Debugger Source Distribution
          # This will catch both haskell-debugger and haskell-debugger-view
          path: dist-newstyle/sdist/haskell-debugger*.tar.gz

  # Tests run on the distributed sdist to catch packaging issues.
  build-n-test-haskell-debugger:
    timeout-minutes: 30
    name: Build and Run Tests on Sdist (GHC ${{ matrix.version }})
    needs: sdist-haskell-debugger
    runs-on: ubuntu-latest # just one is fine here, we test more machines on the checkout
    continue-on-error: ${{ inputs.is-release == false }}
    strategy:
      matrix:
        version: [9.14.1]
        include:
          - channel: https://raw.githubusercontent.com/haskell/ghcup-metadata/master/ghcup-0.0.9.yaml
            version: 9.14.1
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: actions/cache@v3
        with:
          path: |
            ~/.cabal/store
          key: ${{ runner.os }}-integration-ghc-${{ matrix.version }}-cabal-${{ hashFiles('**/*.cabal', 'cabal.project*') }}
          restore-keys: |
            ${{ runner.os }}-integration-ghc-${{ matrix.version }}-cabal-

      - uses: haskell-actions/setup@v2
        with:
          ghc-version: ${{ matrix.version }}
          cabal-version: 3.16
          ghcup-release-channel: ${{ matrix.channel }}

      - name: Download hdb source dist
        uses: actions/download-artifact@v4
        with:
          name: Haskell Debugger Source Distribution
          path: ${{ runner.temp }}/dist

      - name: Untar haskell-debugger
        run: |
          (cd ${{ runner.temp }}/dist && tar xzf "haskell-debugger-${{env.haskellDebuggerVersion}}.tar.gz")

      - name: Setup local version of haskell-debugger-view
        if: env.USE_HASKELL_DEBUGGER_VIEW_FROM_HACKAGE
        run: |
          (cd ${{ runner.temp }}/dist && tar xzf "haskell-debugger-view-${{env.haskellDebuggerViewVersion}}.tar.gz" && mv "haskell-debugger-view-${{env.haskellDebuggerViewVersion}}" "haskell-debugger-${{env.haskellDebuggerVersion}}/haskell-debugger-view")
          (cd ${{ runner.temp }}/dist/haskell-debugger-${{env.haskellDebuggerVersion}} && echo 'packages: . ./haskell-debugger-view' > cabal.project)

      - name: Build and Run tests
        run: |
          echo "Using haskell-debugger-view from hackage: ${{env.USE_HASKELL_DEBUGGER_VIEW_FROM_HACKAGE}}"
          cd ${{ runner.temp }}/dist/haskell-debugger-${{env.haskellDebuggerVersion}}
          cabal run haskell-debugger-test --enable-executable-dynamic --allow-newer=ghc-bignum,containers,time,ghc,base,template-haskell --ghc-options=-Werror

  # Tests run on the git checkout
  build-n-more-test-haskell-debugger:
    timeout-minutes: 30
    name: Build and Run Tests on checkout (${{ matrix.runner }}, GHC ${{ matrix.version }})
    runs-on: ${{ matrix.runner }}
    # Run all jobs in the matrix to the end (IF RELEASE, STOP ON FAILURE)
    continue-on-error: ${{ inputs.is-release == false }}
    strategy:
      matrix:
        runner: [ubuntu-latest, macOS-latest] # windows-latest is broken, see #246
        version: [9.14.1, 10.1.20260817]
        include:
          - channel: https://raw.githubusercontent.com/haskell/ghcup-metadata/master/ghcup-0.0.9.yaml
            version: 9.14.1
          - channel: https://gitlab.haskell.org/ghc/ghcup-metadata/-/raw/updates/ghcup-nightlies-0.0.7.yaml
            version: 10.1.20260817
          - runner: macOS-latest
            asset_arch: aarch64-apple-darwin
          - runner: ubuntu-24.04-arm
            version: 9.14.1
            channel: https://raw.githubusercontent.com/haskell/ghcup-metadata/master/ghcup-0.0.9.yaml
            asset_arch: aarch64-linux-ubuntu2404
          - runner: macos-15-intel
            version: 9.14.1
            channel: https://raw.githubusercontent.com/haskell/ghcup-metadata/master/ghcup-0.0.9.yaml
            asset_arch: x86_64-apple-darwin
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: "Cache GHC for macOS" # See haskell-actions/setup#139
        id: cache-haskell
        uses: actions/cache@v5
        if: matrix.runner == 'macOS-latest'
        with:
          path: ~/.ghcup
          key: ${{ runner.os }}-ghc-${{ matrix.version }}
          restore-keys: ${{ runner.os }}-ghc-${{ matrix.version }}

      - uses: haskell-actions/setup@v2
        id: setup
        with:
          ghc-version: ${{ matrix.version }}
          cabal-version: 3.16
          ghcup-release-channel: ${{ matrix.channel }}

      - name: Configure the build
        run: |
          cabal build all --dry-run \
            --enable-executable-dynamic --enable-tests \
            --enable-benchmarks --disable-documentation
        # The last step generates dist-newstyle/cache/plan.json for the cache key.

      - name: Restore cached dependencies
        uses: actions/cache/restore@v4
        id: cache
        env:
          key: ${{ runner.os }}-ghc-${{ steps.setup.outputs.ghc-version }}-cabal-${{ steps.setup.outputs.cabal-version }}
        with:
          path: ${{ steps.setup.outputs.cabal-store }}
          key: ${{ env.key }}-plan-${{ hashFiles('**/plan.json') }}
          restore-keys: ${{ env.key }}-

      - name: Install dependencies
        # If we had an exact cache hit, the dependencies will be up to date.
        if: steps.cache.outputs.cache-hit != 'true'
        run: cabal build all --only-dependencies

      # Cache dependencies already here, so that we do not have to rebuild them should the subsequent steps fail.
      - name: Save cached dependencies
        uses: actions/cache/save@v4
        # If we had an exact cache hit, trying to save the cache would error because of key clash.
        if: steps.cache.outputs.cache-hit != 'true'
        with:
          path: ${{ steps.setup.outputs.cabal-store }}
          key: ${{ steps.cache.outputs.cache-primary-key }}

      - name: Build and Run Haskell tests
        shell: bash
        run: |
          if [ "$RUNNER_OS" == "Windows" ]; then
            cabal run haskell-debugger-test
          else
            cabal run haskell-debugger-test --enable-executable-dynamic
          fi

      # Linux x86_64 release binaries are built separately, per-distro, by the
      # build-hdb-linux-bindist job below (for glibc/libtinfo/libgmp
      # compatibility across distros) -- this leg only covers macOS and
      # Linux aarch64, which each ship a single bindist.
      - name: Install patchelf
        if: inputs.is-release == true && matrix.version == '9.14.1' && matrix.asset_arch != '' && runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y patchelf

      - name: Package hdb binary for release
        if: inputs.is-release == true && matrix.version == '9.14.1' && matrix.asset_arch != ''
        shell: bash
        env:
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARCH: ${{ matrix.asset_arch }}
          OUT_DIR: dist-bin
        run: ./scripts/mk-bindist.sh

      - name: Upload hdb binary artifact
        if: inputs.is-release == true && matrix.version == '9.14.1' && matrix.asset_arch != ''
        uses: actions/upload-artifact@v4
        with:
          name: hdb-${{ matrix.asset_arch }}
          path: hdb-*.tar.gz

  build-hdb-linux-deb10:
    name: Build hdb Linux (deb10, GHC ${{ matrix.version }})
    if: inputs.is-release == true
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version: [9.14.1]
        include:
          - version: 9.14.1
            channel: https://raw.githubusercontent.com/haskell/ghcup-metadata/master/ghcup-0.0.9.yaml
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build and package hdb (deb10)
        uses: ./.github/actions/bindist-actions/action-deb10
        with:
          stage: BUILD
          version: ${{ matrix.version }}
        env:
          GHCUP_CHANNEL: ${{ matrix.channel }}
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-deb10

      - name: Upload hdb build artifact
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-build-x86_64-linux-deb10-${{ matrix.version }}
          path: out-x86_64-linux-deb10-${{ matrix.version }}.tar

  build-hdb-linux-deb11:
    name: Build hdb Linux (deb11, GHC ${{ matrix.version }})
    if: inputs.is-release == true
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version: [9.14.1]
        include:
          - version: 9.14.1
            channel: https://raw.githubusercontent.com/haskell/ghcup-metadata/master/ghcup-0.0.9.yaml
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build and package hdb (deb11)
        uses: ./.github/actions/bindist-actions/action-deb11
        with:
          stage: BUILD
          version: ${{ matrix.version }}
        env:
          GHCUP_CHANNEL: ${{ matrix.channel }}
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-deb11

      - name: Upload hdb build artifact
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-build-x86_64-linux-deb11-${{ matrix.version }}
          path: out-x86_64-linux-deb11-${{ matrix.version }}.tar

  build-hdb-linux-deb12:
    name: Build hdb Linux (deb12, GHC ${{ matrix.version }})
    if: inputs.is-release == true
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version: [9.14.1]
        include:
          - version: 9.14.1
            channel: https://raw.githubusercontent.com/haskell/ghcup-metadata/master/ghcup-0.0.9.yaml
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build and package hdb (deb12)
        uses: ./.github/actions/bindist-actions/action-deb12
        with:
          stage: BUILD
          version: ${{ matrix.version }}
        env:
          GHCUP_CHANNEL: ${{ matrix.channel }}
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-deb12

      - name: Upload hdb build artifact
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-build-x86_64-linux-deb12-${{ matrix.version }}
          path: out-x86_64-linux-deb12-${{ matrix.version }}.tar

  build-hdb-linux-deb13:
    name: Build hdb Linux (deb13, GHC ${{ matrix.version }})
    if: inputs.is-release == true
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version: [9.14.1]
        include:
          - version: 9.14.1
            channel: https://raw.githubusercontent.com/haskell/ghcup-metadata/master/ghcup-0.0.9.yaml
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build and package hdb (deb13)
        uses: ./.github/actions/bindist-actions/action-deb13
        with:
          stage: BUILD
          version: ${{ matrix.version }}
        env:
          GHCUP_CHANNEL: ${{ matrix.channel }}
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-deb13

      - name: Upload hdb build artifact
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-build-x86_64-linux-deb13-${{ matrix.version }}
          path: out-x86_64-linux-deb13-${{ matrix.version }}.tar

  build-hdb-linux-fedora33:
    name: Build hdb Linux (fedora33, GHC ${{ matrix.version }})
    if: inputs.is-release == true
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version: [9.14.1]
        include:
          - version: 9.14.1
            channel: https://raw.githubusercontent.com/haskell/ghcup-metadata/master/ghcup-0.0.9.yaml
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build and package hdb (fedora33)
        uses: ./.github/actions/bindist-actions/action-fedora33
        with:
          stage: BUILD
          version: ${{ matrix.version }}
        env:
          GHCUP_CHANNEL: ${{ matrix.channel }}
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-fedora33

      - name: Upload hdb build artifact
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-build-x86_64-linux-fedora33-${{ matrix.version }}
          path: out-x86_64-linux-fedora33-${{ matrix.version }}.tar

  build-hdb-linux-fedora40:
    name: Build hdb Linux (fedora40, GHC ${{ matrix.version }})
    if: inputs.is-release == true
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version: [9.14.1]
        include:
          - version: 9.14.1
            channel: https://raw.githubusercontent.com/haskell/ghcup-metadata/master/ghcup-0.0.9.yaml
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build and package hdb (fedora40)
        uses: ./.github/actions/bindist-actions/action-fedora40
        with:
          stage: BUILD
          version: ${{ matrix.version }}
        env:
          GHCUP_CHANNEL: ${{ matrix.channel }}
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-fedora40

      - name: Upload hdb build artifact
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-build-x86_64-linux-fedora40-${{ matrix.version }}
          path: out-x86_64-linux-fedora40-${{ matrix.version }}.tar

  build-hdb-linux-mint202:
    name: Build hdb Linux (mint202, GHC ${{ matrix.version }})
    if: inputs.is-release == true
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version: [9.14.1]
        include:
          - version: 9.14.1
            channel: https://raw.githubusercontent.com/haskell/ghcup-metadata/master/ghcup-0.0.9.yaml
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build and package hdb (mint202)
        uses: ./.github/actions/bindist-actions/action-mint202
        with:
          stage: BUILD
          version: ${{ matrix.version }}
        env:
          GHCUP_CHANNEL: ${{ matrix.channel }}
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-mint202

      - name: Upload hdb build artifact
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-build-x86_64-linux-mint202-${{ matrix.version }}
          path: out-x86_64-linux-mint202-${{ matrix.version }}.tar

  build-hdb-linux-mint213:
    name: Build hdb Linux (mint213, GHC ${{ matrix.version }})
    if: inputs.is-release == true
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version: [9.14.1]
        include:
          - version: 9.14.1
            channel: https://raw.githubusercontent.com/haskell/ghcup-metadata/master/ghcup-0.0.9.yaml
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build and package hdb (mint213)
        uses: ./.github/actions/bindist-actions/action-mint213
        with:
          stage: BUILD
          version: ${{ matrix.version }}
        env:
          GHCUP_CHANNEL: ${{ matrix.channel }}
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-mint213

      - name: Upload hdb build artifact
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-build-x86_64-linux-mint213-${{ matrix.version }}
          path: out-x86_64-linux-mint213-${{ matrix.version }}.tar

  build-hdb-linux-mint222:
    name: Build hdb Linux (mint222, GHC ${{ matrix.version }})
    if: inputs.is-release == true
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version: [9.14.1]
        include:
          - version: 9.14.1
            channel: https://raw.githubusercontent.com/haskell/ghcup-metadata/master/ghcup-0.0.9.yaml
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build and package hdb (mint222)
        uses: ./.github/actions/bindist-actions/action-mint222
        with:
          stage: BUILD
          version: ${{ matrix.version }}
        env:
          GHCUP_CHANNEL: ${{ matrix.channel }}
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-mint222

      - name: Upload hdb build artifact
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-build-x86_64-linux-mint222-${{ matrix.version }}
          path: out-x86_64-linux-mint222-${{ matrix.version }}.tar

  build-hdb-linux-ubuntu2004:
    name: Build hdb Linux (ubuntu2004, GHC ${{ matrix.version }})
    if: inputs.is-release == true
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version: [9.14.1]
        include:
          - version: 9.14.1
            channel: https://raw.githubusercontent.com/haskell/ghcup-metadata/master/ghcup-0.0.9.yaml
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build and package hdb (ubuntu2004)
        uses: ./.github/actions/bindist-actions/action-ubuntu2004
        with:
          stage: BUILD
          version: ${{ matrix.version }}
        env:
          GHCUP_CHANNEL: ${{ matrix.channel }}
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-ubuntu2004

      - name: Upload hdb build artifact
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-build-x86_64-linux-ubuntu2004-${{ matrix.version }}
          path: out-x86_64-linux-ubuntu2004-${{ matrix.version }}.tar

  build-hdb-linux-ubuntu2204:
    name: Build hdb Linux (ubuntu2204, GHC ${{ matrix.version }})
    if: inputs.is-release == true
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version: [9.14.1]
        include:
          - version: 9.14.1
            channel: https://raw.githubusercontent.com/haskell/ghcup-metadata/master/ghcup-0.0.9.yaml
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build and package hdb (ubuntu2204)
        uses: ./.github/actions/bindist-actions/action-ubuntu2204
        with:
          stage: BUILD
          version: ${{ matrix.version }}
        env:
          GHCUP_CHANNEL: ${{ matrix.channel }}
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-ubuntu2204

      - name: Upload hdb build artifact
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-build-x86_64-linux-ubuntu2204-${{ matrix.version }}
          path: out-x86_64-linux-ubuntu2204-${{ matrix.version }}.tar

  build-hdb-linux-ubuntu2404:
    name: Build hdb Linux (ubuntu2404, GHC ${{ matrix.version }})
    if: inputs.is-release == true
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version: [9.14.1]
        include:
          - version: 9.14.1
            channel: https://raw.githubusercontent.com/haskell/ghcup-metadata/master/ghcup-0.0.9.yaml
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build and package hdb (ubuntu2404)
        uses: ./.github/actions/bindist-actions/action-ubuntu2404
        with:
          stage: BUILD
          version: ${{ matrix.version }}
        env:
          GHCUP_CHANNEL: ${{ matrix.channel }}
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-ubuntu2404

      - name: Upload hdb build artifact
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-build-x86_64-linux-ubuntu2404-${{ matrix.version }}
          path: out-x86_64-linux-ubuntu2404-${{ matrix.version }}.tar

  build-hdb-linux-unknown:
    name: Build hdb Linux (unknown, GHC ${{ matrix.version }})
    if: inputs.is-release == true
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version: [9.14.1]
        include:
          - version: 9.14.1
            channel: https://raw.githubusercontent.com/haskell/ghcup-metadata/master/ghcup-0.0.9.yaml
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build and package hdb (unknown)
        uses: ./.github/actions/bindist-actions/action-unknown
        with:
          stage: BUILD
          version: ${{ matrix.version }}
        env:
          GHCUP_CHANNEL: ${{ matrix.channel }}
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-unknown

      - name: Upload hdb build artifact
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-build-x86_64-linux-unknown-${{ matrix.version }}
          path: out-x86_64-linux-unknown-${{ matrix.version }}.tar

  bindist-hdb-linux-deb10:
    name: Bindist hdb Linux (deb10)
    if: inputs.is-release == true
    needs: build-hdb-linux-deb10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Download hdb build artifacts (deb10)
        uses: actions/download-artifact@v4
        with:
          pattern: artifacts-build-x86_64-linux-deb10-*
          merge-multiple: true
          path: ./

      - name: Merge hdb bindist (deb10)
        uses: ./.github/actions/bindist-actions/action-deb10
        with:
          stage: BINDIST
        env:
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-deb10

      - name: Upload hdb binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: hdb-x86_64-linux-deb10
          path: hdb-*.tar.gz

  bindist-hdb-linux-deb11:
    name: Bindist hdb Linux (deb11)
    if: inputs.is-release == true
    needs: build-hdb-linux-deb11
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Download hdb build artifacts (deb11)
        uses: actions/download-artifact@v4
        with:
          pattern: artifacts-build-x86_64-linux-deb11-*
          merge-multiple: true
          path: ./

      - name: Merge hdb bindist (deb11)
        uses: ./.github/actions/bindist-actions/action-deb11
        with:
          stage: BINDIST
        env:
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-deb11

      - name: Upload hdb binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: hdb-x86_64-linux-deb11
          path: hdb-*.tar.gz

  bindist-hdb-linux-deb12:
    name: Bindist hdb Linux (deb12)
    if: inputs.is-release == true
    needs: build-hdb-linux-deb12
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Download hdb build artifacts (deb12)
        uses: actions/download-artifact@v4
        with:
          pattern: artifacts-build-x86_64-linux-deb12-*
          merge-multiple: true
          path: ./

      - name: Merge hdb bindist (deb12)
        uses: ./.github/actions/bindist-actions/action-deb12
        with:
          stage: BINDIST
        env:
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-deb12

      - name: Upload hdb binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: hdb-x86_64-linux-deb12
          path: hdb-*.tar.gz

  bindist-hdb-linux-deb13:
    name: Bindist hdb Linux (deb13)
    if: inputs.is-release == true
    needs: build-hdb-linux-deb13
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Download hdb build artifacts (deb13)
        uses: actions/download-artifact@v4
        with:
          pattern: artifacts-build-x86_64-linux-deb13-*
          merge-multiple: true
          path: ./

      - name: Merge hdb bindist (deb13)
        uses: ./.github/actions/bindist-actions/action-deb13
        with:
          stage: BINDIST
        env:
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-deb13

      - name: Upload hdb binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: hdb-x86_64-linux-deb13
          path: hdb-*.tar.gz

  bindist-hdb-linux-fedora33:
    name: Bindist hdb Linux (fedora33)
    if: inputs.is-release == true
    needs: build-hdb-linux-fedora33
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Download hdb build artifacts (fedora33)
        uses: actions/download-artifact@v4
        with:
          pattern: artifacts-build-x86_64-linux-fedora33-*
          merge-multiple: true
          path: ./

      - name: Merge hdb bindist (fedora33)
        uses: ./.github/actions/bindist-actions/action-fedora33
        with:
          stage: BINDIST
        env:
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-fedora33

      - name: Upload hdb binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: hdb-x86_64-linux-fedora33
          path: hdb-*.tar.gz

  bindist-hdb-linux-fedora40:
    name: Bindist hdb Linux (fedora40)
    if: inputs.is-release == true
    needs: build-hdb-linux-fedora40
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Download hdb build artifacts (fedora40)
        uses: actions/download-artifact@v4
        with:
          pattern: artifacts-build-x86_64-linux-fedora40-*
          merge-multiple: true
          path: ./

      - name: Merge hdb bindist (fedora40)
        uses: ./.github/actions/bindist-actions/action-fedora40
        with:
          stage: BINDIST
        env:
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-fedora40

      - name: Upload hdb binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: hdb-x86_64-linux-fedora40
          path: hdb-*.tar.gz

  bindist-hdb-linux-mint202:
    name: Bindist hdb Linux (mint202)
    if: inputs.is-release == true
    needs: build-hdb-linux-mint202
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Download hdb build artifacts (mint202)
        uses: actions/download-artifact@v4
        with:
          pattern: artifacts-build-x86_64-linux-mint202-*
          merge-multiple: true
          path: ./

      - name: Merge hdb bindist (mint202)
        uses: ./.github/actions/bindist-actions/action-mint202
        with:
          stage: BINDIST
        env:
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-mint202

      - name: Upload hdb binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: hdb-x86_64-linux-mint202
          path: hdb-*.tar.gz

  bindist-hdb-linux-mint213:
    name: Bindist hdb Linux (mint213)
    if: inputs.is-release == true
    needs: build-hdb-linux-mint213
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Download hdb build artifacts (mint213)
        uses: actions/download-artifact@v4
        with:
          pattern: artifacts-build-x86_64-linux-mint213-*
          merge-multiple: true
          path: ./

      - name: Merge hdb bindist (mint213)
        uses: ./.github/actions/bindist-actions/action-mint213
        with:
          stage: BINDIST
        env:
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-mint213

      - name: Upload hdb binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: hdb-x86_64-linux-mint213
          path: hdb-*.tar.gz

  bindist-hdb-linux-mint222:
    name: Bindist hdb Linux (mint222)
    if: inputs.is-release == true
    needs: build-hdb-linux-mint222
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Download hdb build artifacts (mint222)
        uses: actions/download-artifact@v4
        with:
          pattern: artifacts-build-x86_64-linux-mint222-*
          merge-multiple: true
          path: ./

      - name: Merge hdb bindist (mint222)
        uses: ./.github/actions/bindist-actions/action-mint222
        with:
          stage: BINDIST
        env:
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-mint222

      - name: Upload hdb binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: hdb-x86_64-linux-mint222
          path: hdb-*.tar.gz

  bindist-hdb-linux-ubuntu2004:
    name: Bindist hdb Linux (ubuntu2004)
    if: inputs.is-release == true
    needs: build-hdb-linux-ubuntu2004
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Download hdb build artifacts (ubuntu2004)
        uses: actions/download-artifact@v4
        with:
          pattern: artifacts-build-x86_64-linux-ubuntu2004-*
          merge-multiple: true
          path: ./

      - name: Merge hdb bindist (ubuntu2004)
        uses: ./.github/actions/bindist-actions/action-ubuntu2004
        with:
          stage: BINDIST
        env:
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-ubuntu2004

      - name: Upload hdb binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: hdb-x86_64-linux-ubuntu2004
          path: hdb-*.tar.gz

  bindist-hdb-linux-ubuntu2204:
    name: Bindist hdb Linux (ubuntu2204)
    if: inputs.is-release == true
    needs: build-hdb-linux-ubuntu2204
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Download hdb build artifacts (ubuntu2204)
        uses: actions/download-artifact@v4
        with:
          pattern: artifacts-build-x86_64-linux-ubuntu2204-*
          merge-multiple: true
          path: ./

      - name: Merge hdb bindist (ubuntu2204)
        uses: ./.github/actions/bindist-actions/action-ubuntu2204
        with:
          stage: BINDIST
        env:
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-ubuntu2204

      - name: Upload hdb binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: hdb-x86_64-linux-ubuntu2204
          path: hdb-*.tar.gz

  bindist-hdb-linux-ubuntu2404:
    name: Bindist hdb Linux (ubuntu2404)
    if: inputs.is-release == true
    needs: build-hdb-linux-ubuntu2404
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Download hdb build artifacts (ubuntu2404)
        uses: actions/download-artifact@v4
        with:
          pattern: artifacts-build-x86_64-linux-ubuntu2404-*
          merge-multiple: true
          path: ./

      - name: Merge hdb bindist (ubuntu2404)
        uses: ./.github/actions/bindist-actions/action-ubuntu2404
        with:
          stage: BINDIST
        env:
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-ubuntu2404

      - name: Upload hdb binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: hdb-x86_64-linux-ubuntu2404
          path: hdb-*.tar.gz

  bindist-hdb-linux-unknown:
    name: Bindist hdb Linux (unknown)
    if: inputs.is-release == true
    needs: build-hdb-linux-unknown
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Download hdb build artifacts (unknown)
        uses: actions/download-artifact@v4
        with:
          pattern: artifacts-build-x86_64-linux-unknown-*
          merge-multiple: true
          path: ./

      - name: Merge hdb bindist (unknown)
        uses: ./.github/actions/bindist-actions/action-unknown
        with:
          stage: BINDIST
        env:
          HDB_VERSION: ${{ env.haskellDebuggerVersion }}
          ARTIFACT: x86_64-linux-unknown

      - name: Upload hdb binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: hdb-x86_64-linux-unknown
          path: hdb-*.tar.gz
